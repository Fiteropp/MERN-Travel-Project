version: '3.9'
services:
  backend:
    build:
      context: ./server
      dockerfile: Dockerfile.server
    ports:
      - "8080:8080"
    environment:
      - MONGO_URI=mongodb://mongo:27017/travelagency
      - JWT_SECRET=your_jwt_secret
    depends_on:
      - mongo
    volumes:
      - ./server:/app
    command: ["npm", "run", "dev"]

  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile.client
    ports:
      - "5173:5173"
    volumes:
      - ./client:/app
    stdin_open: true
    tty: true
    command: ["npm", "start"]

  mongo:
    image: mongo:latest
    container_name: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
